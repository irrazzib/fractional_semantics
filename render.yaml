services:
  - type: web
    name: fractional-semantics
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m fs_semantics web-app
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
